---
depth_order: 1
---

# jekyll-potion

`jekyll-potion` 에 오신 것을 환영합니다.

`jekyll-potion` 은 [Jekyll](https://jekyllrb.com/) 기반의 [GitHub Pages](https://pages.github.com/) 를 컨텐츠의 훼손 없이 빠로게 적용하기 위한 도구입니다.

아래의 내용을 통해 `jekyll-potion` 에 대한 보다 더 자세히 다룹니다.

## 시작하기

아래의 문서는 `jekyll-potion` 을 설치하는 방법을 다룹니다.

{% link url="/start" %}

## config

아래의 문서는 `jekyll-potion` 의 설정에 대해 다룹니다.

{% link url="/config" %}

## 라이브러리 구조

아래의 문서는 `jekyll-potion` 의 내부 구조에 대해 다룹니다.

{% link url="/anatomy-library" %}

## GitHub Pages 적용

아래의 문서는 [GitHub Pages](https://pages.github.com/) 로 적용시 설정하는 방법에 대해 다룹니다.

{% link url="/apply-github" %}

## 구성요소

아래의 문서는 각 구성요소를 설명하고, `theme` 의 구조, 생성 및 `tag` template 에 대한 변경, 사용 가능한 `processor` 의 목록 및 설정을 다룹니다.

{% link url="/components" %}
